<?xml version="1.0" encoding="utf-8"?>
<!-- Minimal sample app: one launcher activity hosting the sora CodeEditor and
     one non-exported service hosting the Lua language server behind an
     abstract LocalServerSocket("lua-lsp"). No permissions needed: the LSP
     traffic stays inside the app sandbox (abstract unix domain socket). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.LuaLspSample">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".LspServerService"
            android:exported="false" />
    </application>

</manifest>
