local arithmetic = a + b * -c ^ 2
local chained = root.child[index].name
local callResult = object:method(1, "two", fn())
local lambdaValue = lambda (x, y): x and y or nil
local arrayValue = [1, item, call()]
return arithmetic, chained, callResult, lambdaValue, arrayValue
